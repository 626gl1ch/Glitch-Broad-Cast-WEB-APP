name = "glitch-broadcast-api"
main = "index.js"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "production"
# Secrets like SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, GEMINI_API_KEY can be set via wrangler secret put or local env.

[site]
bucket = "../frontend/dist"
